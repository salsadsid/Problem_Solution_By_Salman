#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n,m;
    cin>>n>>m;

    int routes[n];

    for(int i=0;i<n;i++)
    {
        int a;
        cin>>a;

        routes[i]=a;
    }

    

    return 0;
}